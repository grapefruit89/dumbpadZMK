name: Build

on:
  push:
    paths:
      - 'config/**'
      - 'build.yaml'
      - 'west.yml'
  pull_request:
    paths:
      - 'config/**'
      - 'build.yaml'
      - 'west.yml'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
